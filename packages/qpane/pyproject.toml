[build-system]
requires = ["setuptools==83.0.0", "wheel==0.47.0", "setuptools_scm==10.2.0"]
build-backend = "setuptools.build_meta"

[project]
name = "qpane"
dynamic = ["version"]
description = "High-performance Qt raster and vector rendering SDK and viewer"
readme = { file = "README.md", content-type = "text/markdown" }
authors = [
  { name = "Artificial Sweetener", email = "artificialsweetenerai@proton.me" }
]
license = "GPL-3.0-or-later"
requires-python = ">=3.10"
classifiers = [
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Multimedia :: Graphics",
  "Topic :: Scientific/Engineering :: Image Processing",
]
dependencies = [
  "ferrastra>=1.0.0,<2.0.0",
  "PySide6>=6.7.3",
  "numpy>=1.21",
  "psutil>=5.9",
  "typing-extensions>=4.0",
]

[project.urls]
Homepage = "https://artificialsweetener.ai"
Documentation = "https://github.com/Artificial-Sweetener/CuteCanvas/tree/main/packages/qpane/docs"
Repository = "https://github.com/Artificial-Sweetener/CuteCanvas"
Issues = "https://github.com/Artificial-Sweetener/CuteCanvas/issues"

[tool.setuptools.packages.find]
where = ["src"]
include = ["qpane", "qpane.*"]

[tool.setuptools]
package-dir = {"" = "src"}
zip-safe = false

[tool.setuptools.package-data]
qpane = ["*.pyi", "py.typed"]

[tool.setuptools_scm]
root = "../.."
fallback_version = "3.0.0"

[tool.setuptools_scm.tag]
regex = "^qpane-v(?P<version>.+)$"

[tool.setuptools_scm.scm.git]
describe_command = "git describe --dirty --tags --long --abbrev=40 --match qpane-v*[0-9]*"

[tool.semantic_release]
assets = ["packages/qpane/pyproject.toml"]
commit_parser = "conventional-monorepo"
commit_message = "chore(qpane): release QPane {version}\n\nAutomatically generated by python-semantic-release"
logging_use_named_masks = false
no_git_verify = true
tag_format = "qpane-v{version}"
version_toml = []
version_variables = []

[tool.semantic_release.commit_parser_options]
path_filters = ["packages/qpane"]
scope_prefix = "qpane"

[tool.semantic_release.changelog]
mode = "update"

[tool.semantic_release.changelog.default_templates]
changelog_file = "packages/qpane/CHANGELOG.md"

[tool.semantic_release.branches.main]
match = "main"
prerelease = false
prerelease_token = "rc"

[tool.pytest.ini_options]
addopts = ["--basetemp=.pytest-tmp"]
pythonpath = ["src", "tests", "examples"]
testpaths = ["tests"]
markers = [
  "interactive_performance: isolate strict interactive latency probes from shared hardware contention",
  "performance: mark long-running performance benchmarks",
  "swap_benchmark: mark swap latency benchmarks",
]
