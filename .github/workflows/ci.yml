name: CI

on:
  pull_request:
  schedule:
    - cron: "17 13 * * 1"
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

jobs:
  verify:
    uses: ./.github/workflows/verify.yml
